# macOS
.DS_Store

# Local tooling — not part of the site
.devserver.py
.claude/

# Nothing here is built, so there is no build output to ignore.
# js/data/sounds.json is generated by scripts/build-sounds-data.py but is
# committed: the published site has no game install to read it from.
